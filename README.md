# HW1 Intro HTML

## Overview
This is my submission for HW1 Intro HTML assignment.

## Findings
- Added `<header>`, `<section>`, and `<footer>` for structure.
- Included an `<img>` with `src`, `alt`, `width`, and `height` attributes.
- Used `<a>` with `href` and `target="_blank"` to create external links that open in new tabs.
- Added `id` and `class` attributes for ability to have CSS styling.
- No issues were encountered amd all elements rendered as expected in the browser.

## Files
- `index.html`
- `style.css`
- `script.js`

## Hosting

This site is hosted on GitHub (https://j378463.github.io/hw1-intro-html47832467382/).

